### !! Must run with data=image_data  ## Add assertion errors ?
defaults:
  - choice_detect

name: encoder${trainer.exp}
type: choice_detect

loss: ['cce','l2', 'prior', 'KL', 'l1']
im_model: encoder
